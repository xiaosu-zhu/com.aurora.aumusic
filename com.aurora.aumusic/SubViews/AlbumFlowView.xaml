<Page
    x:Class="com.aurora.aumusic.AlbumFlowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:com.aurora.aumusic"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <CollectionViewSource x:Name="AlbumFolwResources" />
        <DataTemplate x:Name="AlbumFlowTemplate" x:DataType="local:AlbumItem">
            <RelativePanel >
                <Image Source="{x:Bind AlbumItem.AlbumArtWork}" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignHorizontalCenterWithPanel="True" Margin="12"/>
                <TextBlock Text="{x:Bind AlbumItem.AlbumName}" />
            </RelativePanel>
        </DataTemplate>
    </Page.Resources>
    <Grid Background="Transparent">
        <ListView x:Name="AlbumsFlowControls" ItemsSource="{x:Bind AlbumFolwResources.View}" ItemTemplate="{StaticResource AlbumFlowTemplate}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <local:AlbumFlowPanel />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>

    </Grid>
</Page>
